import styles from "./NavBar.module.scss";

export default function NavBar() {
    return (
        <nav className={styles.wrapper}>
            algumas ancoras e um dropdown de contato
        </nav>
    )
}